# SmartEd - College Education Management System 🎓

A simple static prototype of a college education management UI built with plain HTML, CSS, and JavaScript.

## ✅ Project Overview
- **Purpose:** Demonstration UI for dashboard, attendance, marks, curriculum, exams, messaging, chat, reports, and logs.
- **Tech:** Vanilla HTML, CSS and JavaScript (no build tools required).

## 🚀 Quick Start
1. Open the project in a browser by double-clicking `Smarteducation-1.html`.
2. Or run a simple local server (recommended) and open the page:

```bash
# From project root
python -m http.server 8000
# Then open http://localhost:8000/Smarteducation-1.html
```

## 🔧 Demo Credentials
- Use any username; password: `123456`
- Demo user types: `student`, `teacher`, `parent` (select the user type before login)

## 📁 Project Structure
- `Smarteducation-1.html` — Main HTML file (UI markup)
- `css/styles.css` — Extracted stylesheet (all styles)
- `js/app.js` — Application logic (login, navigation, page loading)

## 🧾 How to Test
- Open in browser, log in with demo password `123456` and try navigation links (Dashboard, Attendance, Marks, etc.).
- Open DevTools (F12) → Console to check for runtime errors.

## ✍️ Contributing
- Small, focused PRs preferred. If you want UI improvements or bug fixes, open an issue with reproduction steps.

## ⚠️ Notes
- This is a static prototype with sample data in `js/app.js` for demo purposes only.

---
Made with ❤️ — feel free to ask me to add more details (tests, live server instructions, or contribution guidelines).